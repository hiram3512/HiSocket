name: CI

on: [push, pull_request]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build With MSBuild
      run: msbuild ./src/HiSocket.sln -p:Configuration=Release
